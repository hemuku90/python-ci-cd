# Default values for gist-api.
replicaCount: 1

image:
  repository: hemant1/gist-api
  pullPolicy: IfNotPresent
  tag: "latest"
  digest: ""

service:
  type: ClusterIP
  port: 80

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
